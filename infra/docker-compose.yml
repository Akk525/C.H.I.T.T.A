services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chitta
    ports:
      - "5432:5432"
    volumes:
      - chitta_db:/var/lib/postgresql/data

volumes:
  chitta_db:

